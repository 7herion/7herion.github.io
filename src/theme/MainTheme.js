import { createTheme } from '@mui/material/styles';

let MainTheme = createTheme({
    palette: {
        primary: {
            // main: '#0a5480',
            main: '#ffffff',// '#e3e3e3',
        },
        secondary: {
            // main: '#e3e3e3',
            main: '#0a5480',
        },
    },
});

export default MainTheme;